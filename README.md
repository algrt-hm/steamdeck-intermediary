# streamdeck-intermediary

Simple example of providing HTTP endpoints via a locally-run server which are then used to POST data to [Logitech Media Server](https://en.wikipedia.org/wiki/Logitech_Media_Server).

## to build

To build, run 

```shell
go build .
```

in this directory.